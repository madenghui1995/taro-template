/**
 * @Owners cmZhou
 * @Title date 扩展枚举
 */
export enum Bg {
    Default = '#fff',
}

export enum Font {
    Blue = '#108ee9',
}

export enum Tag {
    Blue = '#108ee9',
    Red = '#f50',
}

export enum Link {
    Blue = '#1890ff',
}
